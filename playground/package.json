{
  "name": "playground",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:deploy": "vite build --mode deploy",
    "build:debug": "vite build --mode deploy-debug"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
